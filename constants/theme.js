// Theme file for the onboarding screen

// Contains all the styling in onboarding.js

import { Dimensions } from 'react-native';

const { width, height } = Dimensions.get('screen');

export const COLORS = {
    primary: '#524FAA',
    grey: '#7E7E7E',
};

export const SIZES = {
    h1: 22,
    h2: 28,
    h3: 18,
    h4: 16,
    h5: 14,
    h6: 12,

    width,
    height,
}