import { StyleSheet, Text, View } from "react-native";

type Props = {};

const Gradient = (props: Props) => {
    return(
        <View>
            <Text>Gradient</Text>
        </View>
    );
};

export default Gradient;